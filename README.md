# PureScript Hello World

## Command

```
# Setup
$ npm install

# Run
$ npm exec spago run
or
$ spago run     # if `node_modules/.bin` is in PATH
```

## Packages

- `purescript`
- `purescript-language-server`
- `spago`
